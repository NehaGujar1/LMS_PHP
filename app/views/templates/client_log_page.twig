<head>
<link rel="stylesheet" href="styles/client_log_page.css">
<link rel="stylesheet" href="styles/main.css">
</head>
<pre> </pre>
<h1>CLient login</h1>
<hr>
<form action="/client_log_pg" method="post" class="c1">
<table>
    {% if sp!=null %}<tr id="tit">
    <th class="ss">Book name</th> 
    <th class="ss">Author</th> 
    <th class="ss">Check out</th>
    </tr>{% endif %}
  {% for post in sp %}
  <tbody>
  <tr>
    <td class="ss">{{post.book_name}}</td> 
    <td class="ss">{{post.author}}</td>
    <td><input type="submit" class="input" name="client_log_pg" style="color: white"  value="{{post.isbn}},--{{name}}"></input></td>
  </tr></tbody>
  {% endfor %}
</table>

  <table>
{% if ur!=null %}<tr id="tit3">
    <th>Book name</th>
    <th>Author</th> 
    <th>Check in</th>
    </tr>{% endif %}
  {% for post in ur %}
  <tr>
    <td>{{post.book_name}}</td> 
    <td>{{post.author}}</td>
    <td><input type="submit"  class="input" name="ur_bkk" value="{{post.isbn}},--{{name}}"></input></td>
  </tr>
  {% endfor %}
</table>

{% if sent %}
  <p>
    <em><strong>Successfully</strong> done!</em>
  </p>
  {% endif %}
  <p id="c5">Total fees you have to pay are :{{var.fees}}</p>
  <input type ="submit" name="end" value="Go back"  class="input" id="c3"></input>
  <input type = "submit" value = "Log out" name="Log_out" id="c4" class="input"></input>
</form>